services:

  # open5gs
  core:
    container_name: lte-core
    image: lte-b200-core
    command: ./initCore.sh
    tty: true
    env_file: ".env"
    environment:
      - TIME=$TIME
    build:
      dockerfile: core.Dockerfile
      context: ./docker
      network: host
    privileged: true
    network_mode: "host"
    cap_add:  
      - SYS_NICE
      - NET_ADMIN
      - SYS_ADMIN
    ulimits:
      rtprio: 99
      rttime: -1
      memlock: 8428281856
    healthcheck:
      test: ["CMD-SHELL", "grep -q 'Running LTE Network' ./health.log"]
      interval: 5s
      timeout: 1s
      retries: 60
    volumes:
      - /dev/:/dev/
      - /proc:/proc
      - /lte:/log
  
  # srsran gnb
  enb:
    container_name: lte-enb
    image: lte-b200-enb
    command: ./initEnb.sh
    env_file: ".env"
    environment:
      - TIME=$TIME
    build:
      dockerfile: enb.Dockerfile
      context: ./docker
      network: host
    privileged: true
    network_mode: "host"
    cap_add:
      - SYS_NICE
      - NET_ADMIN
      - SYS_ADMIN
    ulimits:
      rtprio: 99
      rttime: -1
      memlock: 8428281856
    volumes:
      - /dev/:/dev/
      - /proc:/proc
      - /lte:/tmp
    depends_on:
      core:
        condition: service_healthy
        restart: true
    stdin_open: true
    tty: true